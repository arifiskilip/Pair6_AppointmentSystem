namespace Domain.Enums
{
    public enum AppointmentStatusEnum
    {
       Available = 1, // Müsait
       Canceled =2, // İptal edildi
       Completed = 3, // Tamamlandı
       Created = 4, // Randevu Oluştu
    }
}
